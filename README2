git pull origin master --allow-unrelated-histories
git merge origin origin/master

use este comando para poder brincar el error de que no dejaba fusionar
//fatal: refusing to merge unrelated histories.

